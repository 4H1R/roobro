{
	admin off
	auto_https off
}

:3000 {
	encode zstd gzip

	@api path /api/* /health
	handle @api {
		reverse_proxy api:8080
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
